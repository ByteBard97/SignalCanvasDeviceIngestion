# Core dependencies
pydantic>=2.0.0
pydantic-settings>=2.0.0
click>=8.0.0
requests>=2.31.0

# LangGraph for pipeline orchestration
langgraph>=0.0.1
langchain>=0.1.0

# RAG and embeddings
sentence-transformers>=2.2.0
lancedb>=0.1.0

# Development
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=23.0.0
ruff>=0.1.0
mypy>=1.0.0

# Optional: PDF conversion (requires system deps on macOS, runs on Linux via SSH)
# marker-ai>=0.2.0

# Optional: PatchLang compiler (build from sibling repo)
# patchlang-python>=0.2.2
