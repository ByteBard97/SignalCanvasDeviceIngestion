# Session 66fb8190 — 2026-05-25

## Summary

Continued rolling_v20 batch from previous session. Pipeline had stalled due to two critical bugs found and fixed this session.

## Bug 1: Infinite retry loop (CRITICAL)

`audinate-avio-usb-c` had 26,678 failure_attempts because `_query_stage_34_nodes` in `runner.py` was picking up queue=4 devices where `stage_index_rag=STAGE_FAILED (3)` — the device kept getting retried every second with RAGDB_SUBMISSION_ERROR. Several other EXTRACTION_FAILED devices also had 1,000+ attempts.

**Fix:**
- Set `failure_retryable=0` in DB for all queue=4 devices with >5 attempts (immediate stopgap)
- Added `and (n.failure_attempts or 0) < _MAX_STAGE_34_ATTEMPTS` guard to `_query_stage_34_nodes` in `runner.py`
- Added same cap to stage 5 retry query in `pipeline_stages.py`

## Bug 2: Queue=1 stuck devices

7 devices in `QUEUE_1_READY_FOR_PROCESSING` after top-up additions. 3 had PDFs downloaded but `_query_stage_34_nodes` only checks queues 0, 2, 4 (not 1). 4 had stage_download_pdf=STAGE_FAILED with 5 attempts (at attempt cap).

**Fix:**
- Manually advanced the 3 downloaded devices from queue=1 to queue=0 (DB fix)
- Moved the 4 exhausted PDF-download devices to queue=4 (terminal failure)
- Cleared stale failure_category on the 3 advanced devices so stage_34 would submit them to Ragscallion

## Completeness check fixes (carried over from previous session)

- `review_completeness.py`: `dante_adapter_input`/`dante_adapter_output` removed from `_REQUIRED_PORT_CATEGORIES` — QSC DCIO-H and Klark Teknik DN200 were misclassified as dante_adapter_input and kept failing "No Dante network port found"
- With that fix live, reset QSC DCIO-H + Klark Teknik DN200 + Sennheiser HMD 27 (reclassified from wireless_rx to headset) → all three patched successfully
- Robe iFORTE FS reset for re-extraction (parse error in generated patch)

## End state

- Queue 5 (completed): 85+ devices (was 80 at session start)
- Queue 4 (failed): ~31
- Queue 3 (active): 1 (D-San LSP-2 extracting) + Robe iFORTE FS retry pending
- Top-up loop: still halted at 60% failure threshold
- Runner PID 65624 processing final devices

## Pending

- Wait for D-San LSP-2 + Robe iFORTE FS to complete
- Export all completed patches to SignalCanvasDeviceLibrary
- Restart top-up loop (failure rate should drop below 60% once current batch completes)
- Consider whether to continue adding more devices from rolling_v20.txt
